# ModelForge 3D Editor

Browser-based 3D modeling and scene editor.

## PWA / Android packaging

ModelForge is configured for PWA packaging with a web app manifest, service worker, offline app shell, and PNG app icons. The production site is hosted under `/tanvir/` on GitHub Pages.

The Android package can be generated from the deployed PWA with PWABuilder after the live deployment has updated.