import "./pwa-register.js";
import * as THREE from "three";
import { setupUpload } from "./core/upload.js";
import { chooseSaveFormat, saveScene } from "./core/save.js?v=20260811-save-singleton-1";
import { loadScene } from "./core/load.js";
import { setupExporter } from "./core/exporter.js";
import { setupImporter } from "./core/importer.js";
import { setupCopyPaste } from "./core/copyPaste.js";
import { setupDuplicate } from "./core/duplicate.js";
import { toggleSnap, setupTransform, setTransformMode, setTransformSpace, toggleTransformSpace, getTransformSpace, isDraggingTransform } from "./core/transform.js";
import { initNumericTransform } from "./core/numericTransform.js";
import { undo, redo, canUndo, canRedo } from "./core/history.js";
import { initScene, scene, renderer, camera, controls, createDefaultCube, grid, resetCamera, getFPS } from "./core/scene.js?v=20260811-runtime-fix";
import { focusObject } from "./core/focus.js";
import { setupDelete } from "./core/delete.js";
import { setupSelection, clearSelection, getSelected } from "./core/selection.js";
import { addObject, getObjects } from "./core/objectManager.js";
import { addToHierarchy, initHierarchy, rebuildHierarchy } from "./ui/hierarchy.js";
import { initInspector, updateInspector } from "./ui/inspector.js";
import { initSetFocusControl } from "./ui/setFocusControl.js";
import { initPivotControl } from "./ui/pivotControl.js";
import { initMaterialLibrary } from "./core/materialLibrary.js";
import { initMaterialLibraryPanel } from "./ui/materialLibraryPanel.js";
import { createObject } from "./objects/factory.js";
import { groupSelected, ungroupSelected } from "./core/grouping.js";

const state = { transformMode: "translate", transformSpace: "world", lastStatus: "Ready", mobileSheet: null, device: getDeviceProfile() };
boot();

function boot() {
    installAdaptiveStyles(); initScene(); setupTransform(camera, renderer, scene, controls); setupSelection(renderer, camera, scene); initInspector(); initSetFocusControl(); initPivotControl(); initNumericTransform(); initMaterialLibrary(); initHierarchy(scene); setupDelete(scene); setupDuplicate(scene); setupCopyPaste(scene); setupImporter(scene); setupExporter(scene); setupUpload(scene);
    const defaultCube = createDefaultCube(); addToHierarchy(defaultCube); updateInspector(null); bindUI(); bindEditorEvents(); bindKeyboard(); bindResponsiveLayer(); requestAnimationFrame(() => initMaterialLibraryPanel()); updateObjectCount(); updateHistoryButtons(); syncTransformSpaceUI(); setStatus("Ready");
}

function installAdaptiveStyles() {
    if (document.getElementById("modelForgeAdaptiveStyles")) return;
    const style = document.createElement("style"); style.id = "modelForgeAdaptiveStyles";
    style.textContent = `.mobile-panel-btn{display:none!important}#uploadBtn{display:none!important}#mobileBackdrop{position:fixed;inset:0;z-index:35;background:rgba(0,0,0,.58);backdrop-filter:blur(2px)}#mobileBackdrop[hidden]{display:none!important}.transform-space-badge{font:600 10px system-ui,sans-serif;color:#9ea2ac;padding:5px 8px;border:1px solid #30333b;border-radius:5px;background:#17181d;letter-spacing:.3px}.transform-space-btn{min-width:52px!important}@media(max-width:760px){.mobile-panel-btn{display:grid!important}.mobile-sheet{display:flex!important;position:fixed!important;left:0!important;right:0!important;bottom:0!important;top:auto!important;width:100%!important;min-width:0!important;max-height:min(78vh,680px)!important;height:auto!important;z-index:40!important;border:1px solid #3a3d45!important;border-bottom:0!important;border-radius:15px 15px 0 0!important;box-shadow:0 -18px 45px rgba(0,0,0,.62)!important;overflow:hidden!important;transform:translateY(105%);transition:transform .2s ease-out}.mobile-sheet.mobile-sheet-open{transform:translateY(0)}.mobile-sheet .scene-tree-wrap,.mobile-sheet #inspectorContent{min-height:0;overflow:auto}.mobile-sheet .panel-header,.mobile-sheet .mobile-sheet-handle,.mobile-sheet .mobile-sheet-header,.mobile-sheet .left-footer{flex:0 0 auto}body.sheet-open #bottomToolbar{pointer-events:none;opacity:.35}}.material-library-list{display:grid;gap:6px;margin-top:8px}.material-library-item{display:grid;grid-template-columns:1fr auto auto;align-items:center;gap:5px;padding:7px 8px;border:1px solid #30323a;border-radius:5px;background:#17181c}.material-library-item span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:#c9cbd1;font-size:11px}.material-library-item .icon-action{min-width:34px}@media(prefers-reduced-motion:reduce){.mobile-sheet{transition:none!important}}`;
    document.head.appendChild(style);
}

function getDeviceProfile(){const width=window.innerWidth,coarse=window.matchMedia?.("(pointer: coarse)").matches??false;return{width,coarse,mobile:width<=760,tablet:width>760&&width<=1100,desktop:width>1100};}

function bindUI(){
    document.getElementById("sceneAddBtn")?.addEventListener("click",e=>{e.stopPropagation();toggleAddMenu(e.currentTarget)}); document.getElementById("addMenuBtn")?.addEventListener("click",e=>toggleAddMenu(e.currentTarget));
    document.getElementById("undoBtn")?.addEventListener("click",()=>{if(undo())setStatus("Undo")}); document.getElementById("redoBtn")?.addEventListener("click",()=>{if(redo())setStatus("Redo")});
    document.getElementById("saveBtn")?.addEventListener("click",()=>{chooseSaveFormat(scene);});
    document.getElementById("loadBtn")?.addEventListener("click",()=>{loadScene(scene);setStatus("Choose a project or supported 3D file")});
    document.getElementById("gridBtn")?.addEventListener("click",()=>{grid.visible=!grid.visible;setStatus(grid.visible?"Grid enabled":"Grid hidden")}); document.getElementById("frameBtn")?.addEventListener("click",frameSelected); document.getElementById("cameraResetBtn")?.addEventListener("click",()=>{resetCamera();setStatus("View reset")});
    document.getElementById("selectBtn")?.addEventListener("click",()=>activateTool("select")); document.getElementById("moveBtn")?.addEventListener("click",()=>activateTool("translate")); document.getElementById("rotateBtn")?.addEventListener("click",()=>activateTool("rotate")); document.getElementById("scaleBtn")?.addEventListener("click",()=>activateTool("scale"));
    document.getElementById("snapBtn")?.addEventListener("click",()=>{const enabled=toggleSnap(),button=document.getElementById("snapBtn");if(button){button.textContent=enabled?"Snap On":"Snap Off";button.classList.toggle("snap-on",enabled)}});
    document.getElementById("spaceBtn")?.addEventListener("click",()=>{const space=toggleTransformSpace();state.transformSpace=space;syncTransformSpaceUI();setStatus(`${space === "world" ? "World" : "Local"} transform space`)});
    document.getElementById("groupBtn")?.addEventListener("click",()=>{const group=groupSelected(scene);setStatus(group?`Grouped ${group.name}`:"Select two or more objects with Ctrl-click");rebuildHierarchy()}); document.getElementById("ungroupBtn")?.addEventListener("click",()=>{const children=ungroupSelected(scene,getSelected());setStatus(children.length?"Group ungrouped":"Select a group first")}); document.getElementById("addCollectionBtn")?.addEventListener("click",()=>setStatus("Collections are represented by Groups. Ctrl-click objects, then Group.")); document.getElementById("sceneOptionsBtn")?.addEventListener("click",()=>setStatus(`${getObjects().length} editor objects`)); document.getElementById("mobileSceneBtn")?.addEventListener("click",()=>toggleMobileSheet("scene")); document.getElementById("mobileInspectorBtn")?.addEventListener("click",()=>toggleMobileSheet("inspector")); document.getElementById("mobileBackdrop")?.addEventListener("click",closeMobileSheet); document.getElementById("viewportGizmo")?.addEventListener("click",event=>{const axis=event.target.closest("[data-axis]")?.dataset.axis;if(axis)alignView(axis)}); document.addEventListener("pointerdown",event=>{const popover=document.getElementById("menuPopover");if(popover&&!popover.hidden&&!popover.contains(event.target)&&!event.target.closest("#sceneAddBtn,#addMenuBtn"))closeMenu()});
}

function bindEditorEvents(){window.addEventListener("editor:selection-change",event=>{const selection=event.detail||[];if(selection.length===1)setStatus(`Selected ${selection[0].name||selection[0].type}`);else if(selection.length>1)setStatus(`${selection.length} objects selected`);else setStatus("Ready");updateObjectCount()});window.addEventListener("editor:status",event=>setStatus(event.detail||"Ready"));window.addEventListener("editor:hierarchy-refresh",rebuildHierarchy);window.addEventListener("editor:history-change",updateHistoryButtons);window.addEventListener("editor:frame",event=>{const fps=event.detail?.fps??getFPS(),el=document.getElementById("fpsCounter");if(el)el.textContent=fps});window.addEventListener("editor:transform-mode",event=>{state.transformMode=event.detail;syncToolButtons()});window.addEventListener("editor:transform-space",event=>{state.transformSpace=event.detail||getTransformSpace();syncTransformSpaceUI()});window.addEventListener("editor:snap-change",event=>{const button=document.getElementById("snapBtn");if(button){button.textContent=event.detail?"Snap On":"Snap Off";button.classList.toggle("snap-on",!!event.detail)}});window.addEventListener("editor:project-opened",event=>{updateObjectCount();clearSelection();setStatus(`Opened ${event.detail?.fileName||"project"}`)});}

function bindKeyboard(){window.addEventListener("keydown",event=>{const target=event.target,editing=target instanceof HTMLInputElement||target instanceof HTMLTextAreaElement||target instanceof HTMLSelectElement;if(editing)return;const key=event.key.toLowerCase(),mod=event.ctrlKey||event.metaKey;if(mod&&key==="z"){event.preventDefault();if(event.shiftKey?redo():undo())setStatus(event.shiftKey?"Redo":"Undo");return}if(mod&&key==="y"){event.preventDefault();if(redo())setStatus("Redo");return}if(mod&&key==="o"){event.preventDefault();loadScene(scene);return}if(mod&&key==="s"){event.preventDefault();chooseSaveFormat(scene);return}if(key==="w")activateTool("select");if(key==="g")activateTool("translate");if(key==="r")activateTool("rotate");if(key==="s")activateTool("scale");if(key==="f")frameSelected();if(key==="q"){event.preventDefault();const space=toggleTransformSpace();state.transformSpace=space;syncTransformSpaceUI();setStatus(`${space === "world" ? "World" : "Local"} transform space`)}if(key==="escape"){closeMobileSheet();closeMenu();clearSelection()}})}

function bindResponsiveLayer(){const apply=()=>{state.device=getDeviceProfile();document.documentElement.dataset.device=state.device.mobile?"mobile":state.device.tablet?"tablet":"desktop";document.documentElement.dataset.input=state.device.coarse?"touch":"mouse";["mobileSceneBtn","mobileInspectorBtn"].forEach(id=>{const button=document.getElementById(id);if(button)button.style.setProperty("display",state.device.mobile?"grid":"none","important")});if(!state.device.mobile)closeMobileSheet()};window.addEventListener("resize",apply,{passive:true});window.addEventListener("orientationchange",()=>setTimeout(apply,80));apply()};function activateTool(mode){if(mode==="select"){state.transformMode="select";syncToolButtons();setStatus("Select tool");return}setTransformMode(mode);state.transformMode=mode;syncToolButtons();setStatus(`${mode[0].toUpperCase()}${mode.slice(1)} tool`)}function syncToolButtons(){const map={select:"selectBtn",translate:"moveBtn",rotate:"rotateBtn",scale:"scaleBtn"};Object.values(map).forEach(id=>document.getElementById(id)?.classList.remove("active"));document.getElementById(map[state.transformMode])?.classList.add("active")}
function syncTransformSpaceUI(){const button=document.getElementById("spaceBtn"),badge=document.getElementById("transformSpaceBadge");const space=state.transformSpace||getTransformSpace();if(button){button.textContent=space==="world"?"World":"Local";button.title=`Transform Space: ${space}. Shortcut Q`;button.classList.toggle("space-local",space==="local");button.classList.toggle("space-world",space==="world")};if(badge)badge.textContent=space==="world"?"WORLD":"LOCAL"}
function add(type){const object=createObject(type);if(!object)return;addObject(scene,object);addToHierarchy(object);setStatus(`Added ${object.name}`);updateObjectCount()}function toggleAddMenu(anchor){const popover=document.getElementById("menuPopover");if(!popover)return;if(!popover.hidden)return closeMenu();popover.innerHTML=`<button data-add="cube">Cube <span class="shortcut">Shift+C</span></button><button data-add="sphere">Sphere</button><button data-add="cylinder">Cylinder</button><button data-add="cone">Cone</button><button data-add="plane">Plane</button>`;popover.querySelectorAll("[data-add]").forEach(button=>button.addEventListener("click",()=>{add(button.dataset.add);closeMenu()}));const rect=anchor.getBoundingClientRect();popover.style.left=`${Math.min(rect.left,window.innerWidth-205)}px`;popover.style.top=`${rect.bottom+4}px`;popover.hidden=false}function closeMenu(){const popover=document.getElementById("menuPopover");if(popover)popover.hidden=true}function frameSelected(){const object=getSelected();if(!object)return setStatus("Nothing selected");const ok=focusObject(object,{duration:0});setStatus(ok?`Focused ${object.name||object.type}`:"Unable to focus selected object")}function alignView(axis){const target=getSelected(),center=target?new THREE.Box3().setFromObject(target).getCenter(new THREE.Vector3()):controls.target.clone(),distance=target?Math.max(new THREE.Box3().setFromObject(target).getBoundingSphere(new THREE.Sphere()).radius*3,3):6,positions={x:new THREE.Vector3(distance,0,0),y:new THREE.Vector3(0,distance,0),z:new THREE.Vector3(0,0,distance)};camera.position.copy(center).add(positions[axis]);controls.target.copy(center);controls.update();setStatus(`${axis.toUpperCase()} axis view`)}function toggleMobileSheet(type){if(!state.device.mobile)return;if(state.mobileSheet===type)return closeMobileSheet();closeMobileSheet();state.mobileSheet=type;const panel=type==="scene"?document.getElementById("leftPanel"):document.getElementById("rightPanel"),backdrop=document.getElementById("mobileBackdrop");if(!panel||!backdrop)return;ensureSheetHeader(panel,type);panel.classList.add("mobile-sheet","mobile-sheet-open");panel.setAttribute("aria-modal","true");panel.setAttribute("role","dialog");panel.style.setProperty("display","flex","important");backdrop.hidden=false;document.body.classList.add("sheet-open")}function ensureSheetHeader(panel,type){if(panel.querySelector(".mobile-sheet-header"))return;const header=document.createElement("div");header.className="mobile-sheet-header";header.innerHTML=`<div><span class="eyebrow">Mobile workspace</span><strong>${type==="scene"?"Scene":"Inspector"}</strong></div><button class="mobile-sheet-close" aria-label="Close panel">×</button>`;const handle=document.createElement("div");handle.className="mobile-sheet-handle";header.querySelector(".mobile-sheet-close").addEventListener("click",closeMobileSheet);panel.prepend(header);panel.prepend(handle)}function closeMobileSheet(){[document.getElementById("leftPanel"),document.getElementById("rightPanel")].forEach(panel=>{if(!panel)return;panel.classList.remove("mobile-sheet","mobile-sheet-open");panel.style.removeProperty("display")});const backdrop=document.getElementById("mobileBackdrop");if(backdrop)backdrop.hidden=true;document.body.classList.remove("sheet-open");state.mobileSheet=null}function updateObjectCount(){const count=document.getElementById("objectCount");if(count)count.textContent=getObjects().length}function updateHistoryButtons(){const undoButton=document.getElementById("undoBtn"),redoButton=document.getElementById("redoBtn");if(undoButton)undoButton.disabled=!canUndo();if(redoButton)redoButton.disabled=!canRedo()}function setStatus(message){state.lastStatus=message;const text=document.getElementById("statusText");if(text)text.textContent=message}
let longPressTimer=0;document.addEventListener("pointerdown",event=>{if(!state.device?.coarse||!event.target.closest("#viewport"))return;clearTimeout(longPressTimer);longPressTimer=window.setTimeout(()=>{if(isDraggingTransform())return;const selected=getSelected();if(selected)window.dispatchEvent(new CustomEvent("editor:long-press",{detail:selected}))},600)},{passive:true});document.addEventListener("pointerup",()=>clearTimeout(longPressTimer),{passive:true});document.addEventListener("pointercancel",()=>clearTimeout(longPressTimer),{passive:true});